# LVTodo - Геймифицированное приложение для управления задачами

## Описание

LVTodo - это кроссплатформенное мобильное приложение (Android & iOS) для управления задачами в формате игры. Идеально подходит для пар, семей и групп друзей, которые хотят превратить повседневные дела в увлекательную игру с баллами, уровнями и достижениями.

## Основные возможности

### 🎮 Игровая механика
- **Система уровней и опыта** - получайте XP за выполненные задания
- **Баллы и награды** - зарабатывайте баллы и тратьте их на желания
- **Достижения** - разблокируйте значки за различные успехи
- **Две сложности заданий**: легкие (10 баллов) и сложные (25 баллов)

### 📋 Управление задачами
- Создание заданий с выбором исполнителя и срока
- Push-уведомления о новых заданиях
- Автоматические напоминания: 80%, 50%, 30%, 5% от оставшегося времени
- Возможность оспорить задание с новым сроком
- Подтверждение выполнения создателем задания

### 👥 Групповая работа
- Создание и присоединение к группам
- Работа в нескольких группах одновременно
- Простая система приглашений по коду
- Просмотр статистики всех участников

### 🎁 Магазин желаний
- Создание списка желаний
- Покупка желаний за накопленные баллы
- Отслеживание исполненных желаний

### 📊 Статистика и история
- Полная история выполненных заданий
- Среднее время выполнения
- Процент выполненных вовремя
- Подсказки на основе предыдущих заданий

## Технологический стек

- **Frontend**: Flutter (Dart) - кроссплатформенная разработка
- **Backend**: Firebase
  - Authentication - авторизация по email
  - Firestore - база данных реального времени
  - Cloud Messaging - push-уведомления
  - Cloud Functions - серверная логика
- **State Management**: Provider
- **UI/UX**: Material Design с кастомной геймифицированной темой

## Установка и настройка

### Предварительные требования

1. **Flutter SDK** (>=3.0.0)
2. **Firebase CLI**
3. **Android Studio** (для Android) или **Xcode** (для iOS)

### Быстрый старт

1. **Установите зависимости:**
```bash
flutter pub get
```

2. **Настройте Firebase:**
   - Создайте проект в Firebase Console
   - Скачайте `google-services.json` (Android) и `GoogleService-Info.plist` (iOS)
   - Поместите файлы в соответствующие директории
   - Включите Authentication (Email/Password), Firestore, Cloud Messaging

3. **Запустите приложение:**
```bash
flutter run
```

Подробная инструкция по настройке Firebase и развертыванию Cloud Functions находится в разделе ниже.

## Структура проекта

```
lib/
├── main.dart                 # Точка входа
├── models/                   # Модели данных
├── services/                 # Бизнес-логика
├── screens/                  # UI экраны
├── widgets/                  # Переиспользуемые виджеты
└── utils/                    # Утилиты

functions/                    # Cloud Functions
├── index.js
└── package.json
```

## Основные функции

### Регистрация и авторизация
- Email/пароль авторизация через Firebase
- Создание никнейма при регистрации
- Автоматический вход при повторном запуске

### Создание и управление группами
- Создайте группу и получите код приглашения
- Поделитесь кодом с друзьями/семьей
- Присоединитесь к существующим группам по коду

### Работа с заданиями
1. Создайте задание, выбрав исполнителя и срок
2. Исполнитель получит push-уведомление
3. Автоматические напоминания по мере приближения дедлайна
4. Отметка выполнения исполнителем
5. Подтверждение или оспаривание создателем

### Игровая механика
- Зарабатывайте баллы и опыт за выполненные задания
- Повышайте уровень и разблокируйте достижения
- Тратьте баллы на покупку желаний
- Отслеживайте статистику и историю

## Настройка Firebase (подробно)

### 1. Создание проекта

1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Создайте новый проект
3. Включите Google Analytics (опционально)

### 2. Настройка Authentication

1. В Firebase Console откройте **Authentication**
2. Перейдите в **Sign-in method**
3. Включите **Email/Password**

### 3. Настройка Firestore

1. Откройте **Firestore Database**
2. Создайте базу данных в режиме **production**
3. Настройте правила безопасности (см. ниже)

### 4. Настройка для Android

1. Добавьте Android приложение в Firebase
2. Укажите имя пакета: `com.example.lvtodo` (или ваше)
3. Скачайте `google-services.json`
4. Поместите в `android/app/google-services.json`

### 5. Настройка для iOS

1. Добавьте iOS приложение в Firebase
2. Укажите Bundle ID
3. Скачайте `GoogleService-Info.plist`
4. Откройте `ios/Runner.xcworkspace` в Xcode
5. Добавьте файл в проект через Xcode

### 6. Правила безопасности Firestore

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == userId;
    }
    match /groups/{groupId} {
      allow read: if request.auth != null && request.auth.uid in resource.data.memberIds;
      allow create: if request.auth != null;
      allow update: if request.auth != null && request.auth.uid in resource.data.memberIds;
    }
    match /tasks/{taskId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
      allow update: if request.auth != null && (request.auth.uid == resource.data.creatorId || request.auth.uid == resource.data.assigneeId);
    }
    match /wishes/{wishId} {
      allow read, write: if request.auth != null;
    }
    match /achievements/{achievementId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
    }
    match /task_history/{historyId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
    }
  }
}
```

### 7. Развертывание Cloud Functions

```bash
# Инициализация Firebase
firebase init

# Выберите Functions и Firestore
# Установите зависимости
cd functions
npm install

# Разверните функции
firebase deploy --only functions
```

## Игровая система

### Баллы
- Легкое задание: **10 баллов**
- Сложное задание: **25 баллов**
- Просрочка: **минус баллы**

### Уровни
- Экспоненциальный рост требуемого опыта
- Уровень N требует: N × 100 × 1.5 XP
- Только задания, выполненные вовремя, дают XP

### Достижения
1. 🎯 Первый шаг - 1 задание
2. ⭐ Начинающий - 10 заданий
3. 🏆 Опытный - 50 заданий
4. 👑 Мастер - 100 заданий
5. 💯 Идеальная неделя
6. ⚡ Скоростной режим - выполнение за 20% времени
7. 🌟 Уровень 5
8. 💫 Уровень 10
9. 💰 Коллекционер баллов - 1000 баллов

## Troubleshooting

### Firebase не инициализируется
```bash
flutter clean
flutter pub get
flutter run
```

### Push-уведомления не работают
- Проверьте `google-services.json` / `GoogleService-Info.plist`
- Убедитесь что Cloud Functions развернуты
- Проверьте логи: `firebase functions:log`

### Ошибки сборки Android
```bash
cd android && ./gradlew clean && cd ..
flutter clean && flutter pub get
```

### Ошибки сборки iOS
```bash
cd ios && pod deintegrate && pod install && cd ..
flutter clean && flutter pub get
```

## Сборка релизной версии

### Android (APK)
```bash
flutter build apk --release
```

### Android (App Bundle для Google Play)
```bash
flutter build appbundle --release
```

### iOS
```bash
flutter build ios --release
# Затем откройте Xcode для архивации
```

## Будущие улучшения

- [ ] Темная тема
- [ ] Мультиязычность
- [ ] Аватары пользователей
- [ ] Чат в группах
- [ ] Повторяющиеся задания
- [ ] Web версия

## Лицензия

MIT License

---

**Превратите свои дела в игру! 🎮🎯**