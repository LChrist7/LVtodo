# Быстрый старт: Email уведомления ⚡

Все уже настроено! Осталось только задеплоить функции.

## Шаг 1: Установка Firebase CLI (если еще не установлен)

```bash
npm install -g firebase-tools
firebase login
```

## Шаг 2: Установка Cloud Functions (если еще не установлены)

```bash
cd functions
npm install
```

## Шаг 3: Настройка для продакшена

Ваш Gmail App Password: `awzi eqpr ixfx kjzw`

**Установите секрет для пароля:**

```bash
firebase functions:secrets:set SMTP_PASSWORD --project lvtodo
```

Когда спросит "Please enter a value for SMTP_PASSWORD", введите:
```
awzieqprixfxkjzw
```
(без пробелов!)

## Шаг 4: Деплой с параметрами

```bash
# Из корневой директории
firebase deploy --only functions --project lvtodo \
  --set-env-vars SMTP_USER=levanuas7@gmail.com

# ИЛИ из папки functions
cd functions
firebase deploy --only functions --project lvtodo \
  --set-env-vars SMTP_USER=levanuas7@gmail.com
```

**Примечание:** SMTP_HOST и SMTP_PORT уже имеют значения по умолчанию (Gmail), поэтому их указывать не нужно.

## Шаг 5: Проверка ✅

1. Создайте новое задание в приложении
2. Исполнитель получит email уведомление!

## Локальное тестирование (опционально)

Если хотите сначала протестировать локально:

```bash
# Из корневой директории
npm run emulator
```

Откройте http://localhost:5000 и создайте задание - email отправится реально!

## Что произойдет после деплоя

При создании нового задания:
- ✅ Функция `sendTaskNotification` автоматически сработает
- ✅ Email будет отправлен на почту исполнителя
- ✅ Email будет содержать: название, описание, заказчика, группу, дедлайн, сложность, награду
- ✅ Красивый HTML шаблон с кнопкой "Открыть задание"

При одобрении желания:
- ✅ Функция `sendWishApprovalNotification` автоматически сработает
- ✅ Email будет отправлен на почту создателя желания
- ✅ Email будет содержать: название, описание, стоимость, группу

## Проверка логов

```bash
firebase functions:log --project lvtodo
```

## Стоимость

Firebase Functions имеют бесплатный tier:
- 2 миллиона вызовов/месяц
- 400,000 GB-секунд compute time

Для вашего проекта это будет **бесплатно**!

## Готово! 🎉

Email уведомления настроены и готовы к работе.

Подробная документация: [EMAIL_SETUP.md](EMAIL_SETUP.md)
